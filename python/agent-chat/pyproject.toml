[project]
name = "og-agent-chat"
version = "0.1.7"
requires-python = ">=3.11"
dependencies = [
  "dspy-ai>=3.1.3",
  "trackio>=0.15.0",
  "unidiff>=0.7.5",
]

[project.optional-dependencies]
test = [
  "pytest>=8.4,<9",
]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
